<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.mas.letsteach">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.CenterAnimation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ProfileFragment"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ProfileFragmentOrg"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RequestLayout"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HomePageActivity"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".HomePageVol"
            android:label="@string/title_activity_home_page_vol"
            android:theme="@style/AppTheme" />
        <activity android:name=".RequestFormActivity" />
        <activity
            android:name=".RequestFeedVol"
            android:label="@string/title_activity_request_feed_vol"
            android:theme="@style/AppTheme.Placeholder" />
        <activity android:name=".FilterScreen"></activity>
        <activity
            android:name=".HomePageOrg"
            android:label="@string/title_activity_home_page_org"
            android:theme="@style/AppTheme"></activity><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>